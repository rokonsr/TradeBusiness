@using System.Web.UI.WebControls
@using System.Web.UI.WebControls.WebParts
@{
    //Layout = null;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    ViewBag.Title = "User Information Details";
}

<style>
    span.field-validation-error {
        color: red;
    }
</style>

<div style="width:90%; margin:0 auto" class="tablecontainer">
    <a class="popup btn btn-primary" href="/Admin/UserInfo/Save/0" style="margin-bottom:20px; margin-top:20px;">Add New Admin User</a>
    <table id="myDatatable">
        <thead>
            <tr>
                <th>Username</th>
                <th>Branch Name</th>
                <th>IsActive</th>
                <th>Edit</th>
                <th>Delete</th>
            </tr>
        </thead>
    </table>
</div>

<script>
        $(document).ready(function() {
            var oTable = $('#myDatatable').DataTable({
                "ajax": {
                    "url": '/UserInfo/GetUserInfo',
                    "type": "get",
                    "datatype": "json"
                },

                "columns": [
                    { "data": "Username", "autoWidth": true },
                    { "data": "BranchName", "autoWidth": true },
                    { "data": "IsActive", "autoWidth": true },
                    //{ "data": "MaxCompany", "autoWidth": true },
                    //{ "data": "MaxBranch", "autoWidth": true },
                    {
                        "data": "UserId",
                        "width": "50px",
                        "render": function(data) {
                            return '<a class="popup" href="/Admin/UserInfo/Edit/' + data + '">Edit</a>';
                        }
                    },
                    {
                        "data": "UserId",
                        "width": "50px",
                        "render": function(data) {
                            return '<a class="popup" href="/Admin/UserInfo/Delete/' + data + '">Delete</a>';
                        }
                    }
                ]
            });
            $('.tablecontainer').on('click', 'a.popup', function(e) {
                e.preventDefault();
                OpenPopup($(this).attr('href'));
            });

            function OpenPopup(pageUrl) {
                var $pageContent = $('<div/>');
                $pageContent.load(pageUrl, function() {
                    $('#popupForm', $pageContent).removeData('validator');
                    $('#popupForm', $pageContent).removeData('unobtrusiveValidation');
                    $.validator.unobtrusive.parse('form');

                });

                $dialog = $('<div class="popupWindow" style="overflow:auto; padding:10px 30px;"></div>')
                    .html($pageContent)
                    .dialog({
                        draggable: true,
                        autoOpen: false,
                        resizable: true,
                        model: true,

                        title: '@ViewBag.Title',
                        height: 525,

                        title: '@ViewBag.Title',
                        height: 525,

                        title: '@ViewBag.Title',
                        height: 600,

                        width: 600,
                        close: function () {

                            $dialog.dialog('destroy').remove();
                        }
                    });

                $('.popupWindow').on('submit', '#popupForm', function(e) {
                    var url = $('#popupForm')[0].action;
                    $.ajax({
                        type: "POST",
                        url: url,
                        data: $('#popupForm').serialize(),
                        success: function(data) {
                            if (data.status) {
                                $dialog.dialog('close');
                                oTable.ajax.reload();
                            }
                        }
                    });

                    e.preventDefault();
                });

                $dialog.dialog('open');
            }
        })
</script>